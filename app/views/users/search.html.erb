<%= render 'shared/header', message: "Search Results", link: false, path: false %>

<ul>
  <% @users.each do |user| %>
    <li><h4><%= link_to user.username, user_path(user) %></h4></li>
  <% end %>
</ul>
